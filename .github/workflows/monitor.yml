name: Monitor Websites

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Call monitor endpoint
        run: |
          curl https://pingwatch.fazelshah.fun/api/monitor
